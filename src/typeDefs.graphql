type Query {
  infoQuery: String!
  authorQuery(authorIndex: Int!): Author!
  authorListQuery: [Author!]!
  bookQuery: Book!
}

type Author {
  id: ID!
  name: String
  biography: String
  books: [Book!]!
}

type Book {
  id: ID!
  name: String
  description: String
  author: Author!
}
