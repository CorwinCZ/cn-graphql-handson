type Query {
  infoQuery: String!
  authorQuery: Author!
  authorListQuery: [Author!]!
  bookQuery: Book!
}

type Author {
  id: ID!
  name: String
  biography: String
}

type Book {
  id: ID!
  name: String
  description: String
  author: Author!
}
